extends ./templates/template

block vars
    - var pageTitle = 'Lipsum'
    - var main = true

block content
    main.content
        +intro({
            mix: 'content__block',
        })

    +modal({
        mod: 'price-order'
    })
        +price-order({})
